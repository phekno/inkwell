# tui

Bubble Tea TUI client for inkwell. Dark mode is automatic via `lipgloss.AdaptiveColor`.

## Run

```sh
go run ./cmd/inkwell
```

## Build a release binary

```sh
go build -o bin/inkwell ./cmd/inkwell
```

## Keys

- `q` / `esc` / `ctrl+c` — quit
