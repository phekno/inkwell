# Go
*.test
*.out
/api/bin/
/tui/bin/
/api/bootstrap
/tui/bootstrap
/tui/inkwell-dev
/api/import
/api/lambda.zip

# Notion export (local import source — not part of the repo)
/Export-*/

# Node / Vue
node_modules/
dist/
.vite/
*.log

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local env (web/.env is committed — it only has public Cognito IDs)
.env.local
*.local
